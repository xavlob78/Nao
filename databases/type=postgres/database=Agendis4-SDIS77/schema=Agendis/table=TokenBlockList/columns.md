# TokenBlockList

**Dataset:** `Agendis`

## Columns (5)

- id (int32 NOT NULL)
- jti (string(36) NOT NULL)
- type (string(16) NOT NULL)
- user_id (int32 NOT NULL)
- created_at (timestamp(6) NOT NULL)
